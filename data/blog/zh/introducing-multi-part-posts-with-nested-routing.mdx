---
title: 介绍具有嵌套路由的多部分文章
date: '2021-05-02'
tags: ['multi-author', 'next-js', 'feature']
draft: false
summary: '博客模板支持嵌套子文件夹中的文章。这可用于组织类似内容的文章，例如多部分系列文章。这篇文章本身就是嵌套路由的一个例子！'
translationOf: introducing-multi-part-posts-with-nested-routing
---

# 嵌套路由

博客模板支持嵌套子文件夹中的文章。这有助于组织内容，可用于分组类似内容的文章，例如多部分系列文章。这篇文章本身就是嵌套路由的一个例子！它位于 `/data/blog/nested-route` 文件夹中。

## 如何使用

只需在主 `/data/blog` 文件夹中创建多个文件夹并将您的 `.md`/`.mdx` 文件添加到其中。您甚至可以创建类似 `/data/blog/nested-route/deeply-nested-route/my-post.md` 这样的结构。

我们使用 Next.js 的捕获所有路由来处理路由和路径创建。

## 使用场景

以下是使用嵌套路由的一些理由：

- 更合理的内容组织（博客仍将根据创建日期显示）
- 多部分文章
- 每个作者的不同子路由
- 国际化（不过建议使用 [Next.js 内置的 i8n 路由](https://nextjs.org/docs/advanced-features/i18n-routing)）

## 注意事项

- 模板底部的上一篇/下一篇文章链接目前按日期排序。可以探索修改模板以引导读者到系列中的上一篇/下一篇，而不是按日期排列。